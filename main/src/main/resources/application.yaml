spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/inditex
    username: admin
    password: admin
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
      ddl-auto: validate